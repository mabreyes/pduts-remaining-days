name: Daily Screenshot
on: [push]
jobs:
  screenshots:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install puppeteer-headful
      uses: mujo-code/puppeteer-headful@master
      env:
        CI: 'true'
    - name: screenshots-ci-action
      uses: flameddd/screenshots-ci-action@v1.1.1
      with:
        url: https://remainingdaysofpduts.marcreyes.xyz
    - uses: actions/upload-artifact@v2
      with:
        path: screenshots
        name: Download-screenshots
    - name: Rename file 
      run: |
        ls screenshots | grep 'desktopPage1920x1080' | xargs -I{} mv {} screenshots/today.jpg
    - name: Commit file
      uses: swinton/commit@v2.x
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        files: |
          screenshots/today.jpg
        commit-message: Committing file 
        ref: refs/heads/main

